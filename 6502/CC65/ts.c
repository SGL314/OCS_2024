#include <stdio.h>

void main() {
    // Função padrão para imprimir uma string
    printf("Hello, 6502 World!\n");

    // Exemplo de loop simples
    for (int i = 0; i < 5; i++) {
        printf("Loop iteration: %d\n", i);
    }

    // Finaliza o programa
    printf("Program has ended.\n");
}
